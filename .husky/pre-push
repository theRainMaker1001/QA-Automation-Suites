# Pre-Push Gate: Smoke Tests for Integration Breaking Changes
# Catches connectivity issues before they reach PR
# Hard Gate - Will block any fails

echo "Pre-push: typecheck + smoke tests"

npm run typecheck || exit 1
npm run lint || exit 1

# Run smoke tests only (fast, connectivity check)
npm run test:smoke || exit 1

echo "Pre-push gate passed."
