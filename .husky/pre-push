#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

echo "🔎 pre-push: typecheck, lint, api tests"
npm run typecheck || exit 1
npm run lint || exit 1
npm run test:api || exit 1
# If you want E2E on push (slower), uncomment:
# npm run test:e2e || exit 1
echo "✅ pre-push checks passed"
